language: node_js

node_js:
  - '10'

before_script:
  - npm install

deploy:
  provider: npm
  email: '$NPM_EMAIL'
  api_key: '$NPM_TOKEN'
  skip_cleanup: true
  on:
    tags: true

branches:
  only:
    - master
